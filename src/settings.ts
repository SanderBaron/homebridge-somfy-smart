/**
 * Platform-naam zoals geregistreerd bij Homebridge en gebruikt in config.json
 * onder `platforms[].platform`.
 */
export const PLATFORM_NAME = 'SomfySmart';

/**
 * npm-pakketnaam — moet exact overeenkomen met `name` in package.json.
 */
export const PLUGIN_NAME = 'homebridge-somfy-smart';
